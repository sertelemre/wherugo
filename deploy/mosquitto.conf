# WherUGo demo Mosquitto yapılandırması (CONTRACTS §13, --profile mqtt).
#
# !!! YALNIZ DEMO !!!
# Anonim erişim yalnız yerel compose demosu içindir. ÜRETİMDE:
#   - listener 8883 + mTLS (cafile/certfile/keyfile, require_certificate true),
#     cihaz başına istemci sertifikası,
#   - allow_anonymous false + tenant bazlı topic ACL
#     (cihaz yalnız kendi t/{tenant}/{store}/{device}/events konusuna yazabilir),
#   - üretim yolu Mosquitto -> EMQX geçişi (docs/02).
listener 1883
allow_anonymous true
